package pages;

import core.WebDriverExtention;
import org.openqa.selenium.WebDriver;

public class AdminLoginPage extends WebDriverExtention {
    public AdminLoginPage(WebDriver driver) {
        super(driver);
    }
}
